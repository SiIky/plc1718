// Ler um inteiro N, depois ler N numeros e escrever o menor deles
(= Int N (readint))
(= Int i 1)
(= Int tmp 0)
(= Int m (readint))

// le N numeros pro arr
while (< i N) {
    (= tmp (readint))
    (= m (? (< tmp m) tmp m))
    (= i (++ i))
}

(write m)
