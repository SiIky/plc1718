// ler N (constante do programa) numeros e calcular e
// imprimir o seu produtorio
(= Int N 5)
(= Int i 1)
(= Int p (readint))
(= Int tmp 0)

// le N numeros pro arr
while (< i N) {
    (= tmp (readint))
    (= p (* p tmp))
    (= i (++ i))
}

(write p)
